package algorithm;

public class Util {

    /**
     * Get a random int between two integers.
     * @param min - Inclusive (can also be picked).
     * @param max - Exclusive (cannot be picked).
     * @return
     */
    public static int randInt(int min, int max) {return 0;}

    /**
     * Get a random double between two doubles.
     * @param min - Inclusive (can also be picked).
     * @param max - Exclusive (cannot be picked).
     * @return
     */
    public static double randDouble(double min, double max) {return 0;}

}
